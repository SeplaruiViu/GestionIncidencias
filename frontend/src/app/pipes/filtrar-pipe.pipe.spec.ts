import { FiltrarPipePipe } from './filtrar-pipe.pipe';

describe('FiltrarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPipePipe();
    expect(pipe).toBeTruthy();
  });
});
